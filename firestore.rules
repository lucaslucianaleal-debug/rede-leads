rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Permissive rules for testing only — DO NOT use in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
